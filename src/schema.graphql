### This file was generated by Nexus Schema
### Do not make changes to this file directly


enum AgeRating {
  R_12
  R_16
  R_18
}

input CoverInput {
  file: String!
  lang: Int!
}

input DescriptionInput {
  lang: Int!
  text: String!
}

type Language {
  id: Int!
  name: String!
}

type Manga {
  added: String!
  ageRating: String!
  artist: [People]!
  author: [People]!
  cover: [MangaCover]!
  description: [MangaDescription]!
  id: Int!
  publisher: [Publisher]!
  title: [MangaTitle]!
  year: Int
}

type MangaCover {
  file: String!
  id: Int!
  lang: Int!
  langCodes: Language
  mangaId: Int!
}

type MangaDescription {
  id: Int!
  lang: Int!
  langCodes: Language
  mangaId: Int!
  text: String!
}

enum MangaSorting {
  LATEST_UPDATES
  NEW
  POPULARITY
}

enum MangaStatus {
  finish
  ongoing
  release
}

type MangaTitle {
  id: Int!
  lang: Int!
  langCodes: Language
  mangaId: Int!
  text: String!
}

type Mutation {
  createLanguage(name: String!): Language!
  createManga(age_rating: AgeRating!, artist: [PeopleInput!], author: [PeopleInput!], cover: [CoverInput!]!, description: [DescriptionInput!]!, publisher: [PublisherInput!], status: MangaStatus!, title: [TitleInput!]!, year: Int!): Manga!
  createPeople(name: String!): People!
  createPublisher(name: String!): Publisher!
}

type People {
  id: Int!
  name: String!
}

input PeopleInput {
  id: Int!
}

type Publisher {
  id: Int!
  name: String!
}

input PublisherInput {
  id: Int!
}

type Query {
  manga(id: Int!, langId: [Int]): Manga!
  mangaList(direction: SortingDirection, genres: String, langId: [Int], limit: Int, page: Int, sorting: MangaSorting): [Manga!]!
}

enum SortingDirection {
  DESC
}

input TitleInput {
  lang: Int!
  text: String!
}