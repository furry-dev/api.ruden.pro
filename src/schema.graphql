### This file was generated by Nexus Schema
### Do not make changes to this file directly


enum AgeRating {
  R_12
  R_16
  R_18
}

input CoverInput {
  file: String!
  lang: Int!
}

input DescriptionInput {
  lang: Int!
  text: String!
}

type Language {
  id: Int!
  name: String!
}

type Manga {
  added: String!
  age_rating: String!
  cover: [MangaCover]!
  description: [MangaDescription]!
  id: Int!
  title: [MangaTitle]!
  year: Int
}

type MangaCover {
  file: String!
  id: Int!
  lang: Int!
  langCodes: Language
  mangaId: Int!
}

type MangaDescription {
  id: Int!
  lang: Int!
  langCodes: Language
  mangaId: Int!
  text: String!
}

enum MangaSorting {
  LATEST_UPDATES
  NEW
  POPULARITY
}

enum MangaStatus {
  finish
  ongoing
  release
}

type MangaTitle {
  id: Int!
  lang: Int!
  langCodes: Language
  mangaId: Int!
  text: String!
}

type Mutation {
  createManga(age_rating: AgeRating!, cover: [CoverInput!]!, description: [DescriptionInput!]!, status: MangaStatus!, title: [TitleInput!]!, year: Int!): Manga!
}

type Query {
  mangaList(direction: SortingDirection, genres: String, langId: [Int], limit: Int, page: Int, sorting: MangaSorting): [Manga!]!
}

enum SortingDirection {
  DESC
}

input TitleInput {
  lang: Int!
  text: String!
}